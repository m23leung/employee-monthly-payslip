/****************************************************
 * Purpose: Stores the user input commands
 *****************************************************/
let commandList = {
  MONTHLY_SLIP: "generatemonthlypayslip",
};

export default Object.freeze(commandList);
